# AI Courtroom Game Frontend

Next.js App Router scaffold with `src/app`, `src/components`, `src/lib`, and `src/types`.

## Run locally

```bash
cd frontend
npm install
npm run dev
```

Copy `.env.example` to `.env.local` if the backend API URL changes.
